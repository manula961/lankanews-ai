import {ensureSchema,getSql} from "./db";
export type Category="Politics"|"Business"|"Sports"|"Technology"|"Crime"|"Health"|"Education"|"Weather"|"Entertainment"|"Science"|"World"|"Other";
export type Article={id:string;title:string;summary:string;source:string;sourceUrl:string;articleUrl:string;category:Category;publishedAt:string;clusterId?:string|null};
let memory:Article[]=[];
function map(r:any):Article{return{id:r.id,title:r.title,summary:r.summary,source:r.source,sourceUrl:r.source_url,articleUrl:r.article_url,category:r.category,publishedAt:new Date(r.published_at).toISOString(),clusterId:r.cluster_id??null}}
export async function getArticles(){const sql=getSql();if(!sql)return [...memory].sort((a,b)=>+new Date(b.publishedAt)-+new Date(a.publishedAt));await ensureSchema();const rows=await sql("SELECT id,title,summary,source,source_url,article_url,category,published_at,cluster_id FROM articles ORDER BY published_at DESC LIMIT 300");return rows.map(map)}
export async function addArticles(xs:Article[]){const sql=getSql();if(!sql){const ids=new Set(memory.map(a=>a.id));xs.forEach(a=>{if(!ids.has(a.id))memory.push(a)});return}await ensureSchema();for(const a of xs){await sql({query:"INSERT INTO articles (id,title,summary,source,source_url,article_url,category,published_at,cluster_id) VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9) ON CONFLICT (article_url) DO UPDATE SET summary=EXCLUDED.summary,category=EXCLUDED.category",params:[a.id,a.title,a.summary,a.source,a.sourceUrl,a.articleUrl,a.category,a.publishedAt,a.clusterId??null]})}}
export async function stats(){const a=await getArticles();return{total:a.length,sources:new Set(a.map(x=>x.source)).size,recent:a.filter(x=>Date.now()-+new Date(x.publishedAt)<3600000).length,categories:Object.fromEntries([...new Set(a.map(x=>x.category))].map(c=>[c,a.filter(x=>x.category===c).length]))}}