import {NextRequest,NextResponse} from "next/server";import {ingest} from "@/lib/rss";export const maxDuration=60;async function run(req:NextRequest){if(process.env.INGEST_SECRET&&req.headers.get("authorization")!==`Bearer ${process.env.INGEST_SECRET}`)return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(await ingest())}export async function GET(req:NextRequest){return run(req)}export async function POST(req:NextRequest){return run(req)}