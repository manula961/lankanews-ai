import {NextResponse} from "next/server";import {getArticles} from "@/lib/store";export async function GET(req:Request){const u=new URL(req.url);const p=Math.max(1,Number(u.searchParams.get("page")||1));const s=Math.min(100,Math.max(10,Number(u.searchParams.get("size")||30)));const a=await getArticles();return NextResponse.json({page:p,size:s,total:a.length,articles:a.slice((p-1)*s,p*s)})}