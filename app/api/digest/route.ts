import{NextRequest,NextResponse}from"next/server";import{getArticles}from"@/lib/store";export async function GET(req:NextRequest){const hours=Number(req.nextUrl.searchParams.get("hours")||12),cut=Date.now()-hours*3600000,a=(await getArticles()).filter(x=>+new Date(x.publishedAt)>=cut),groups=new Map<string,any[]>();for(const x of a){if(!groups.has(x.category))groups.set(x.category,[]);groups.get(x.category)!.push(x)}return NextResponse.json({hours,total:a.length,sections:[...groups.entries()].map(([category,stories])=>({category,count:stories.length,stories:stories.slice(0,5)})).sort((x,y)=>y.count-x.count)})}