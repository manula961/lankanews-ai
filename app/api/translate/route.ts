import{NextResponse}from"next/server";export async function POST(req:Request){const{article,target}=await req.json();if(!article||!target)return NextResponse.json({error:"article and target required"},{status:400});if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:"Translation service not configured. Add OPENAI_API_KEY."},{status:503});const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:"Bearer "+process.env.OPENAI_API_KEY,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-5.6-luna",input:"Translate this news text to "+target+". Preserve names, numbers, attribution and uncertainty. Return only the translation.\n"+article})});const d=await r.json();return NextResponse.json({translation:d.output_text||""})}