import{NextRequest,NextResponse}from"next/server";import{getArticles}from"@/lib/store";function ok(req:NextRequest){const k=process.env.API_KEY;return !k||req.headers.get("x-api-key")===k||req.nextUrl.searchParams.get("api_key")===k}export async function GET(req:NextRequest){if(!ok(req))return NextResponse.json({error:"Unauthorized"},{status:401});const data=await getArticles();return NextResponse.json({data,meta:{count:data.length,version:"1"}})}