"use client";import{useEffect,useState}from"react";export default function Emergency(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch("/api/articles").then(r=>r.json()).then(x=>setD((x.articles||x).filter((a:any)=>["Weather","Crime","Health"].includes(a.category)))},[]);return <main className="toolPage"><h1>Disaster & Emergency Monitor</h1><p>Recent weather, crime and health reports. Verify urgent information with original publishers or authorities.</p><div className="archiveList">{d.slice(0,30).map(a=><article key={a.id}><b>{a.category}</b><h3>{a.title}</h3><p>{a.summary}</p></article>)}</div></main>}