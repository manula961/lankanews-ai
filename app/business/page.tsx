"use client";import{useEffect,useState}from"react";export default function Business(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch("/api/articles?category=Business").then(r=>r.json()).then(x=>setD(x.articles||x||[]))},[]);return <main className="toolPage"><h1>Business Intelligence</h1><div className="archiveList">{d.map(a=><article key={a.id}><small>{a.source}</small><h3>{a.title}</h3><p>{a.summary}</p></article>)}</div></main>}