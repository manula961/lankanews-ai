"use client";import{useEffect,useState}from"react";export default function Weather(){const[x,setX]=useState<any>();useEffect(()=>{fetch("https://api.open-meteo.com/v1/forecast?latitude=6.9271&longitude=79.8612&current=temperature_2m,relative_humidity_2m,wind_speed_10m,weather_code&timezone=Asia%2FColombo").then(r=>r.json()).then(setX).catch(()=>{})},[]);return <main className="toolPage"><a href="/">← LankaNews AI</a><h1>Weather monitor</h1><p>Current Colombo conditions from Open-Meteo. Use official Sri Lankan authorities for warnings.</p>{x?.current&&<div className="weatherCard"><b>{x.current.temperature_2m}°C</b><span>Humidity {x.current.relative_humidity_2m}%</span><span>Wind {x.current.wind_speed_10m} km/h</span><span>Weather code {x.current.weather_code}</span></div>}</main>}