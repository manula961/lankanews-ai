"use client";import{useEffect,useState}from"react";export default function Timeline(){const[x,setX]=useState<any[]>([]);useEffect(()=>{fetch("/api/timeline").then(r=>r.json()).then(j=>setX(j.items||[]))},[]);return <main className="toolPage"><a href="/">← LankaNews AI</a><h1>Story Timeline</h1><p>Related reports grouped by the current clustering heuristic.</p>{x.map(g=><section className="timeline" key={g.cluster}><h2>{g.title}</h2>{g.stories.map((s:any)=><article key={s.id}><time>{new Date(s.publishedAt).toLocaleString("en-LK")}</time><b>{s.source}</b><p>{s.title}</p><a href={s.articleUrl} target="_blank" rel="noreferrer">Original →</a></article>)}</section>)}</main>}