"use client";import{useEffect,useState}from"react";export default function Districts(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch("/api/map").then(r=>r.json()).then(x=>setD(x.districts||[]))},[]);return <main className="toolPage"><h1>District Intelligence</h1><div className="districtGrid">{d.map(x=><div className="districtCard" key={x.name}><b>{x.name}</b><strong>{x.count}</strong><small>indexed stories</small></div>)}</div></main>}