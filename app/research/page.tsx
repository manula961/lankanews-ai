"use client";import{useState}from"react";export default function Research(){const[q,setQ]=useState("");const[r,setR]=useState<any>();async function go(){setR({loading:true});const x=await fetch("/api/research",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({query:q})});setR(await x.json())}return <main className="toolPage"><h1>Research Mode</h1><p>Search indexed Sri Lankan reporting and build a source-backed brief.</p><div className="researchBox"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="e.g. fuel prices, tourism, education"/><button onClick={go}>Research</button></div>{r?.loading&&<p>Searching…</p>}{r?.sources&&<div className="archiveList">{r.sources.map((a:any)=><article key={a.id}><b>{a.source}</b><h3>{a.title}</h3><p>{a.summary}</p><a href={a.articleUrl} target="_blank">Original →</a></article>)}</div>}</main>}