"use client";import{useEffect,useState}from"react";export default function Trends(){const[d,setD]=useState<any[]>([]);useEffect(()=>{fetch("/api/trends").then(r=>r.json()).then(x=>setD(x.trends||[]))},[]);return <main className="toolPage"><h1>Topic Trends</h1><p>Topic activity from indexed stories.</p><div className="trendGrid">{d.map(x=><div className="trendCard" key={x.topic}><b>{x.topic}</b><strong>{x.count}</strong><small>{x.velocity}/hr</small></div>)}</div></main>}