"use client";import{useEffect,useState}from"react";export default function Archive(){const[d,setD]=useState<any>({articles:[]});useEffect(()=>{fetch("/api/archive?size=50").then(r=>r.json()).then(setD)},[]);return <main className="toolPage"><h1>Historical Archive</h1><p>{d.total||0} indexed stories</p><div className="archiveList">{(d.articles||[]).map((a:any)=><article key={a.id}><small>{new Date(a.publishedAt).toLocaleString()} · {a.source} · {a.category}</small><h3>{a.title}</h3><p>{a.summary}</p><a href={a.articleUrl} target="_blank">Original story →</a></article>)}</div></main>}