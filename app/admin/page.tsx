"use client";import{useState}from"react";export default function Admin(){const[k,setK]=useState(""),[out,setOut]=useState("");async function run(){const r=await fetch("/api/ingest",{method:"POST",headers:{Authorization:"Bearer "+k}});setOut(await r.text())}return <main className="toolPage"><a href="/">← LankaNews AI</a><h1>Admin collector</h1><p>Run a protected collection cycle. Set CRON_SECRET to secure this endpoint.</p><input value={k} onChange={e=>setK(e.target.value)} placeholder="CRON secret"/><button onClick={run}>Run collection</button><pre>{out}</pre></main>}