"use client";import{useEffect,useState}from"react";export default function Entities(){const[e,setE]=useState<any[]>([]);useEffect(()=>{fetch("/api/entities").then(r=>r.json()).then(j=>setE(j.entities||[]))},[]);return <main className="toolPage"><a href="/">← LankaNews AI</a><h1>People, companies & places</h1><p>Heuristic entity index from collected headlines and summaries.</p><div className="entityGrid">{e.map(x=><a href={"/entities/"+encodeURIComponent(x.name)} key={x.type+x.name}><small>{x.type}</small><b>{x.name}</b><span>{x.count} stories</span></a>)}</div></main>}