"use client";import{useEffect,useState}from"react";import{useParams}from"next/navigation";export default function Entity(){const p=useParams<{name:string}>(),[x,setX]=useState<any>({articles:[]});useEffect(()=>{fetch("/api/entities/"+p.name).then(r=>r.json()).then(setX)},[p.name]);return <main className="toolPage"><a href="/entities">← Entities</a><h1>{x.name}</h1>{x.articles.map((a:any)=><article className="entityStory" key={a.id}><b>{a.source}</b><h2>{a.title}</h2><p>{a.summary}</p><a href={a.articleUrl} target="_blank" rel="noreferrer">Original →</a></article>)}</main>}