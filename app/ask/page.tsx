"use client";import{useState}from"react";export default function Ask(){const[q,setQ]=useState(""),[answer,setAnswer]=useState(""),[sources,setSources]=useState<any[]>([]),[busy,setBusy]=useState(false);async function ask(){setBusy(true);const r=await fetch("/api/ask",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({question:q})}),j=await r.json();setAnswer(j.answer||j.error);setSources(j.sources||[]);setBusy(false)}return <main className="toolPage"><a href="/">← LankaNews AI</a><h1>Ask LankaNews AI</h1><p>Ask questions about currently collected Sri Lankan news. Answers use only retrieved story records.</p><div className="askBox"><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="What are the main business stories today?"/><button onClick={ask} disabled={busy||!q}>{busy?"Thinking…":"Ask"}</button></div>{answer&&<section className="answer"><h2>Answer</h2><p>{answer}</p>{sources.map((s,i)=><a key={s.url} href={s.url} target="_blank" rel="noreferrer">[{i+1}] {s.title} — {s.source}</a>)}</section>}</main>}