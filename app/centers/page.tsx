"use client";import{useEffect,useState}from"react";export default function Centers(){const[a,setA]=useState<any[]>([]);useEffect(()=>{fetch("/api/articles").then(r=>r.json()).then(j=>setA(j.articles||[]))},[]);const cats=["Sports","Business","Technology","Health","Education","Weather","Entertainment","Crime"];return <main className="toolPage"><a href="/">← LankaNews AI</a><h1>News Centers</h1><div className="centerGrid">{cats.map(c=><section key={c}><small>{c}</small><h2>{a.filter(x=>x.category===c).length} stories</h2>{a.filter(x=>x.category===c).slice(0,4).map(x=><a href={x.articleUrl} target="_blank" key={x.id}>{x.title}</a>)}</section>)}</div></main>}